# Sublime Text support

1. Copy `Fun.sublime-syntax` into `Packages/User/`.
2. Sublime will auto-detect `.fn` files.

## Theme

1. Copy `FunWeb.sublime-color-scheme` into `Packages/User/`.
2. Open **Preferences: Select Color Scheme** and choose **Fun Web**.
