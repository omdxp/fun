# Vim support

1. Copy `editors/vim` into `~/.vim` (or add it to `runtimepath`).
2. Open a `.fn` file to enable syntax highlighting.

## Theme

Use the bundled website-matching colorscheme:

```vim
colorscheme funweb
```

Or the light variant:

```vim
colorscheme funweblight
```
