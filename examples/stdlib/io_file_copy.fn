// Real-world-ish: write a short message to a file, then report bytes.

imp std.c.io;
imp std.fs;

fun main() {
  Fs f = fs();
  f.write_string("out.txt", "hello from fun\n");
  if f.copy_file("out.txt", "out_copy.txt") {
    printf("copied out.txt -> out_copy.txt\n");
  }
}
