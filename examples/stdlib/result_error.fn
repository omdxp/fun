imp std.c.io;
imp std.result;
imp std.error;

fun main() {
  Error e = error_new_kind(.Invalid, 7, "bad input");
  e.with_context("field: age");

  Result<num> r = err_error(e, 0);
  printf("ok=%d val=%d err=%d %s ctx=%s\n", r.is_ok(), r.value, r.err.code, r.err.message, r.err.context);
}
