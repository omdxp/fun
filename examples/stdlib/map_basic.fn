// Map helpers example.

use std.c.io;
use std.map;

fun main() {
  Map<str, str> m;
  m.init(8);
  m.put("name", "fun");
  m.put("lang", "Fun");

  printf("name=%s\n", m.get("name"));
  printf("lang=%s\n", m.get("lang"));

  m.free();
}
