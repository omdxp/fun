// IO helpers example.

use std.c.io;
use std.fs;

fun main() {
  str path = "tmp_fun_io.txt";
  Fs f = fs();
  f.write_string(path, "hello from fun\n");
  str data = f.read_to_string(path);
  if data != 0 {
    printf("read: %s", data);
  }
}
