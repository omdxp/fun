// Real-world-ish: print the current time (epoch seconds).

imp std.c.io;
imp std.c.mem;
imp std.time;
fun main() {
  Timestamp t = now();
  printf("epoch_seconds=%d\n", t.epoch);
  str buf = malloc(64);
  if buf != 0 {
    t.to_string(buf, 64);
    printf("time_str=%s", buf);
  }
}
