imp std.c.io;
imp std.channel;

fun main() {
  Channel<num> ch = channel_new(0);

  num send_rc = ch.send(42);
  if send_rc != 0 {
    printf("send failed\n");
    ret;
  }

  num value = ch.recv();
  printf("channel value: %lld\n", value);

  _ = ch.destroy();
}
