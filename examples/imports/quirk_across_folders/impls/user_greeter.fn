use std.c.io;
use ..defs.user;
use ..defs.greeter;

pub impl User as Greeter {
  greet(str prefix) void {
    printf("%s %s\n", prefix, self.name);
  }

  bye() void {
    printf("bye %s\n", self.name);
  }
}
