// Character classification and conversion (ctype.h)
// 
// C functions take an `int` (typically an unsigned char promoted); modeled as num.
// Returns are C-style: nonzero means true, 0 means false.

// Checks if the character is alphanumeric.
pub fun isalnum(num c) num;

// Checks if the character is alphabetic.
pub fun isalpha(num c) num;

// Checks if the character is blank (space or tab).
pub fun isblank(num c) num;

// Checks if the character is a control character.
pub fun iscntrl(num c) num;

// Checks if the character is a decimal digit.
pub fun isdigit(num c) num;

// Checks if the character has a graphical representation.
pub fun isgraph(num c) num;

// Checks if the character is lowercase.
pub fun islower(num c) num;

// Checks if the character is printable.
pub fun isprint(num c) num;

// Checks if the character is punctuation.
pub fun ispunct(num c) num;

// Checks if the character is whitespace.
pub fun isspace(num c) num;

// Checks if the character is uppercase.
pub fun isupper(num c) num;

// Checks if the character is a hexadecimal digit.
pub fun isxdigit(num c) num;

// Converts a character code to lowercase, or returns c unchanged if no conversion applies.
pub fun tolower(num c) num;

// Converts a character code to uppercase, or returns c unchanged if no conversion applies.
pub fun toupper(num c) num;
